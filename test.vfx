require task.vfx

stage as

\ gamew gameh 2. 2. 2p/ at *bouncer named ball {
\     *generic {
\         :noname act> 5.0 after> parent @ { 0.1 0.1 sx v+! } 2. after> parent @ kill ; execute
\     }
\     *generic {
\         :noname 1 perform> parent @ as 5 0 do cr ." BIG" 1. 1. sx v! 0.5 wait cr ." SMALL" 2. 2. sx v! 0.5 wait loop ; execute
\     }
\ }

fget tool/aed

\ editmode on

repl hide

c: *box draw> 50 50 blue rectf ;

fget obj/sam
stage { 200 200 at *sam { } }